# Nova-Polaris Skills Registry

Tagline: Route. Ground. Resolve.

## Active skills

| Skill | When to use in this project | Phase |
|---|---|---|
| `aws-skills` | CloudFormation, IAM, S3 and S3-Vectors, AgentCore Runtime, Memory, Gateway ops | 0, 3, 4 |
| `backend-patterns` | Strands `@tool` design, DynamoDB access, retry with backoff, structured logging | 1 |
| `frontend-design` | Ops Console visual direction and distinctive aesthetics | 6 |
| `impeccable` | Product console build: `teach` → `shape` → `craft`, then `polish` and `harden` | 6 |
| `hallmark` | Default design flow plus `audit` and `redesign` for console and README hero; enforces structural variety | 6 |
| `ui-ux-pro-max` | Accessibility (4.5:1, 44px, labels), layout, forms, charts for dashboard | 6 |
| `make-interfaces-feel-better` | Polish pass: concentric radius, tabular numerals, transition scope, hit areas | 6 |

## Load at build time

| Skill | When to trigger |
|---|---|
| `security-review` | Guardrail plus adversarial suite review |
| `e2e-testing` | Playwright flows for console (chat, timeline, inspector) |
| `dashboard-builder` | CloudWatch dashboard and alarms |
| `production-audit` | Pre-submit readiness check |

## Rules

`impeccable` needs `PRODUCT.md` and `DESIGN.md` context first. `hallmark` picks macrostructure plus theme first, states picks out loud, stamps CSS. Tokens by name only (`var(--*)`), never inline hex. Roman display only. Bans: gradient text, glassmorphism default, side-stripe borders, hero-metric template, identical card grids, modal-first.
